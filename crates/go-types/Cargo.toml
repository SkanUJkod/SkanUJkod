# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "go-types"
version = "0.1.5"
authors = ["oxfeeefeee <pb01005051@gmail.com>"]
description = "The type checker of the Goscript project."
keywords = [
    "golang",
    "type-checker",
    "goscript",
]
categories = [
    "compiler",
    "type-checker",
]
license = "BSD-3-Clause"
repository = "https://github.com/oxfeeefeee/goscript/"

[dependencies.go-parser]
path = "../go-parser"

[dependencies.num-bigint]
version = "0.4"

[dependencies.num-rational]
version = "0.4"

[dependencies.num-traits]
version = "0.2"

[dependencies.ordered-float]
version = "3.0"

[dev-dependencies.regex]
version = "1"

[features]
btree_map = ["go-parser/btree_map"]
default = []

# TODO(kidq330) - figure out how to set this for workspace instead of per-crate
[lints.clippy]
pedantic = "warn"
nursery = "warn"